#pragma once

namespace Duality {

    // Unity Canvas render modes -- see CanvasComponent.
    enum class CanvasRenderMode {
        ScreenSpaceOverlay = 0,
        ScreenSpaceCamera = 1,
        WorldSpace = 2,
    };

}
